display_name: EF Core Persistence Architect
short_description: Build EF Core repository + factory + UoW patterns safely.
default_prompt: Use $ef-core-persistence-architect to analyze my C#/.NET solution and safely implement or refactor EF Core persistence using a generic repository, repository factory, and unit of work while reusing existing project patterns.
